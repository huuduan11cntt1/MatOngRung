<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="huuduan.sanpham.matongrung">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconmatongrung"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconmatongrung"
        android:supportsRtl="true"
        android:theme="@style/Theme.MậtOngRừng">
        <activity
            android:name=".MainDatHang"
            android:exported="false" />
        <activity
            android:name=".MainFinish"
            android:exported="false" />
        <activity
            android:name=".MainReviewMatOng"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>